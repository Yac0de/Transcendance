apiVersion: 1

datasources:
     - name: Prometheus  # Nom de la source de données
       type: prometheus  # Type de la source de données
       access: proxy  # Mode d'accès à la source de données
       url: http://prometheus:9090  # URL de l'instance Prometheus
       isDefault: true  # Définit cette source comme la source par défaut
